# Python dependencies. NOTE: ffmpeg (and ffprobe) is a SYSTEM dependency,
# install it via your OS package manager (e.g. apt install ffmpeg) — not pip.
deepgram-sdk==5.3.0
python-dotenv
pyyaml            # student config.yaml + the active-selection pointer (config.py)
pandas
matplotlib
ebooklib          # epub parsing (extract_chapter.py)
beautifulsoup4    # XHTML parsing (extract_chapter.py)
pydantic>=2.0       # schema for the opt-in LLM error review (analyze.py --review)
claude-agent-sdk    # default --review backend: uses your Claude subscription (no API key)
openai>=1.50        # only for --review-backend openai (paid API; needs OPENAI_KEY)
